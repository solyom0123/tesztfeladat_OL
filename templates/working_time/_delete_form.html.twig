<form method="post" action="{{ path('working_time_delete', {'id': working_time.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ working_time.id) }}">
    <button class="btn">Delete</button>
</form>
